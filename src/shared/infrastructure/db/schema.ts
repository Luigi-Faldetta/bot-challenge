// Drizzle schema — single source of truth for all tables.
// Domain code never imports from this file; only infrastructure repositories do.
// Tables get added per bounded context as they are introduced (conversations,
// messages, screenings). Empty for now — the foundation just proves the wiring.

export {};
